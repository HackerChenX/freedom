"""
增强选股策略基类模块

此模块已迁移到unified_base_strategy.py，保留此文件用于向后兼容
"""

from strategy.unified_base_strategy import UnifiedBaseStrategy, PeriodConfig

# 向后兼容的别名
Enhanced_base_strategy = UnifiedBaseStrategy  # 主要别名
EnhancedBaseStrategy = UnifiedBaseStrategy    # 标准命名别名
ENHANCED_BASE_STRATEGY = UnifiedBaseStrategy  # 常量别名