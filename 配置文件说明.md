# VnPy Freedom 配置文件统一说明

## 📁 配置文件整合完成

✅ **已删除重复配置**：`Database_Interfaces/Time_Series_Databases/vnpy_clickhouse/vt_setting_clickhouse.json`

✅ **统一使用主配置**：项目根目录的 `vt_setting.json`

## 🎯 当前配置说明

### 主配置文件：`vt_setting.json`

```json
{
    "font.family": "微软雅黑",
    "font.size": 12,

    "log.active": true,
    "log.level": 20,
    "log.console": true,
    "log.file": true,

    "email.server": "smtp.qq.com",
    "email.port": 465,
    "email.username": "",
    "email.password": "",
    "email.sender": "",
    "email.receiver": "",

    "datafeed.name": "",
    "datafeed.username": "",
    "datafeed.password": "",

    "database.timezone": "Asia/Shanghai",
    "database.name": "clickhouse",
    "database.database": "stock",
    "database.host": "localhost",
    "database.port": 8123,
    "database.user": "default",
    "database.password": "123456"
}
```

## 🔧 配置项说明

### 界面设置
- `font.family`: 界面字体
- `font.size`: 字体大小

### 日志设置
- `log.active`: 启用日志
- `log.level`: 日志级别 (10=DEBUG, 20=INFO, 30=WARNING, 40=ERROR)
- `log.console`: 控制台输出
- `log.file`: 文件输出

### 邮件通知
- `email.*`: 邮件服务器配置（用于交易通知）

### 数据源配置
- `datafeed.name`: 数据源名称（rqdata、tushare等）
- `datafeed.username`: 数据源用户名
- `datafeed.password`: 数据源密码

### 数据库配置
- `database.name`: 数据库类型（clickhouse、sqlite、mysql等）
- `database.timezone`: 时区设置
- `database.host`: 数据库主机
- `database.port`: 数据库端口
- `database.user`: 数据库用户名
- `database.password`: 数据库密码
- `database.database`: 数据库名称

## 🔄 切换数据库类型

### 使用SQLite（简单测试）
```json
{
    "database.name": "sqlite",
    "database.database": "vnpy_data.db"
}
```

### 使用ClickHouse（高性能）
```json
{
    "database.name": "clickhouse",
    "database.database": "stock",
    "database.host": "localhost", 
    "database.port": 8123,
    "database.user": "default",
    "database.password": "123456"
}
```

### 使用MySQL
```json
{
    "database.name": "mysql",
    "database.database": "vnpy_trading",
    "database.host": "localhost",
    "database.port": 3306,
    "database.user": "vnpy",
    "database.password": "your_password"
}
```

## 📊 数据源配置示例

### 米筐数据 (RQData)
```json
{
    "datafeed.name": "rqdata",
    "datafeed.username": "your_phone",
    "datafeed.password": "your_password"
}
```

### TuShare数据
```json
{
    "datafeed.name": "tushare", 
    "datafeed.username": "your_token",
    "datafeed.password": ""
}
```

## 🔒 配置文件安全

### 密码保护
- 不要在代码仓库中提交包含真实密码的配置文件
- 可以使用环境变量或单独的密钥文件

### 示例：使用环境变量
```bash
export VNPY_DB_PASSWORD="your_secure_password"
export VNPY_DATAFEED_TOKEN="your_api_token"
```

在代码中读取：
```python
import os
password = os.getenv("VNPY_DB_PASSWORD", "default_password")
```

## 🧪 验证配置

运行测试脚本验证配置是否正确：

```bash
python3.11 test_data_service.py
```

## 🚀 启动应用

配置完成后启动VnPy：

```bash
python3.11 run_vnpy_freedom.py
```

---

**注意**：修改配置文件后需要重启VnPy应用才能生效。
